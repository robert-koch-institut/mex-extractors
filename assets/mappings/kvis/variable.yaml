# yaml-language-server: $schema=../../__schema__/VariableMapping.json

#belongsTo:
#  - mappingRules:
#      - rule:
#dataType:
#  - fieldInPrimarySource: DatatypeDescription
#    locationInPrimarySource: Mex.vKVISVariables
#    examplesInPrimarySource:
#      - Globally Unique Identifier
#      - Datumswert (Datum + Uhrzeit) nach XML Spezifikation
#      - Integer (Ganzzahl)
#      - Datum (Format DD.MM.YYYY)
#    mappingRules:
#      - rule: Extract original value.
#    comment:
#      There are two fields for datatype. In the field 'Datatype' are the actual datatypes (e.g. GUID, AN, XMLDATETIME, INT, DATE)
#      but the labels may not be as meaningful to users as the description of the datatypes, which is provided in the field 'DatatypeDescription'.
#description:
#  - fieldInPrimarySource: FieldDescription
#    locationInPrimarySource: Mex.vKVISVariables
#    examplesInPrimarySource:
#      - eindeutiger Hash der erzeugten Datei (Nutzung zum Zusammenführen der idat und mdat Daten)
#      - Pseudonym des Patienten nach Rki-Pseudynimisierungs-Verfahren (erzeugt die Pseudonymisierungs-App)
#      - Geschlecht des Patienten
#      - Datum der verabreichten Impfung
#    mappingRules:
#      - rule: Extract original value. Assign 'de' as language for text object.
#    comment: null
hadPrimarySource:
  - mappingRules:
    - forValues:
identifierInPrimarySource:
  - mappingRules:
    - forValues:
label:
  - mappingRules:
    - forValues:
usedIn:
  - mappingRules:
    - forValues:
valueSet:
  - fieldInPrimarySource: FieldValue
    mappingRules:
    - forValues:
      - STRING
  - fieldInPrimarySource: FieldValueLongText
    mappingRules:
    - forValues:
      - BOOL
